import React from "react";
import { createRoot } from "react-dom/client";
import PehchaanDashboard from "./PehchaanDashboard.jsx";

createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <PehchaanDashboard />
  </React.StrictMode>
);
